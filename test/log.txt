Mon.:
 - 9 tests passed
 - What happend: 
    - def calculate_hand(self, who):
        score = 0
        aces = 0
        for c in who.hand:
            if c.rank == "A":
                score += 11
                aces += 1
            elif c.rank in ["J", "Q", "K"]: / elif c.rank is "J" or "Q" or "K":
                score += 10
            else:
                score += int(c.rank)

        while score > 21 and aces > 0:
            score -= 10
            aces -= 1
        return score